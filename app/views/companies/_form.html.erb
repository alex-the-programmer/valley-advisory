<%= form_for(company) do |f| %>
  <% if company.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(company.errors.count, "error") %> prohibited this company from being saved:</h2>

      <ul>
      <% company.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

    <%= f.fields_for :address do |address| %>
      <div class="field">
        <%= address.label :street_address1 %>
        <%= address.text_field :street_address1 %>
      </div>

      <div class="field">
        <%= address.label :street_address2 %>
        <%= address.text_field :street_address2 %>
      </div>

      <div class="field">
        <%= address.label :city %>
        <%= address.text_field :city %>
      </div>

      <div class="field">
        <%= address.label :county%>
        <%= address.text_field :county %>
      </div>

      <div class="field">
        <%= address.label :state %>
        <%= address.text_field :state %>
      </div>

      <div class="field">
        <%= address.label :country %>
        <%= address.text_field :country %>
      </div>

      <div class="field">
        <%= address.label :zip%>
        <%= address.text_field :zip %>
      </div>
    <% end %>

  <div class="field">
    <%= f.label :cloudbase_url %>
    <%= f.text_field :cloudbase_url %>
  </div>

  <div class="field">
    <%= f.label :linkedin_url %>
    <%= f.text_field :linkedin_url %>
  </div>

  <div class="field">
    <%= f.label :glassdoors_url %>
    <%= f.text_field :glassdoors_url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
