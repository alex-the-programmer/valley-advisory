<h1>Editing Candidate</h1>

<%= render 'form', candidate: @candidate %>

<% unless current_user.role == 'candidate' %>
  <%= link_to 'Show', @candidate %> |
  <%= link_to 'Back', candidates_path %>
<% end %>